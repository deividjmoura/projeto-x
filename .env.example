# Backend - Database connection (PostgreSQL via Neon)
# Copie e cole sua string de conexão do Neon aqui
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Backend - JWT signing secret para autenticação
# IMPORTANTE: Mude isso em produção para algo mais seguro
JWT_SECRET=your-secret-key-here

# Backend - Port (porta onde o servidor Express vai rodar)
PORT=4000

# Frontend - URL base para chamadas da API
# Em desenvolvimento: http://localhost:4000
# Em produção: https://seu-dominio.com/api
VITE_API_BASE=http://localhost:4000
